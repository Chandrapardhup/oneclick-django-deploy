script: |
  cd ~/oneclick-django-deploy

  git fetch --all
  git reset --hard origin/main
  git clean -fd

  sudo docker-compose down -v
  sudo docker-compose build --no-cache
  sudo docker-compose up -d